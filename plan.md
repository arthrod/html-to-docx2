1. **Analyze target:** In `src/helpers/html-parser.ts`, the array spread operator `...` is used within `Array.push()` at lines 491, 496, and 499. Based on Bolt's memory: "In V8/Bun hot paths, merging fragment arrays using `Array.push(...items)` introduces call stack size risks for large documents and is significantly slower (~3x) than using a standard `for` loop to push items individually."
2. **Implement fix:** Use `replace_with_git_merge_diff` to replace `normalizedNodes.push(...(array || []))` with `for` loops in `src/helpers/html-parser.ts`. Ensure an inline comment `// ⚡ Bolt: Avoid spread operator in hot path to prevent call stack size exceeded errors and improve performance.` is added to justify the micro-optimization, adhering to the memory constraints.
3. **Verification:** I will verify the replacement worked by running `cat src/helpers/html-parser.ts`. Then, I will run format and lint commands (`bun x oxfmt --write src/helpers/html-parser.ts` and `bun run lint`). Then, I will run the targeted tests if they exist, or the full test suite (`bun test tests/`) to ensure no regressions are introduced.
4. **Pre-commit:** Complete pre-commit steps to ensure proper testing, verification, review, and reflection are done.
5. **Submit PR:** I will create a PR with the title "⚡ Bolt: [performance improvement]" (e.g. "⚡ Bolt: Avoid spread operator in html-parser normalization to prevent stack overflows and improve speed") and include the required description sections: 💡 What, 🎯 Why, 📊 Impact, and 🔬 Measurement.
